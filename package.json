{
  "name": "pcdapp-mockup",
  "private": true,
  "type": "module",
  "scripts": {
    "snapshot": "node scripts/snapshot.mjs",
    "rewrite": "node scripts/rewrite-links.mjs",
    "scrub": "node scripts/scrub-html.mjs",
    "build": "npm run rewrite && npm run fix && npm run scrub",
    "fix": "node scripts/fix-static-html.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0"
  }
}
